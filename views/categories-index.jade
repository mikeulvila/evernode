extends layout

block content
  .spacer
  .container
    .row
      .col-lg-8.col-lg-offset-2.col-md-10.col-md-offset-1
        h1= title
        br
        ul(class='list-group')
          each category in categories
            li(class='list-group-item')
              a(href='/categories/#{category._id}')= category.name
        .spacer
        a(href="/categories/new") Add new category.


